# Hot cache — never a truth source, never committed. See context/memory-bank-schema.md.
memory-bank/.local/

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/

# Node
node_modules/
dist/

# OS / editor noise
.DS_Store
Thumbs.db
