# Step 5 — memory bank update

1. Update `memory-bank/tasks/<slug>.md`: check off this phase in the roadmap, note any
   deviation from what the spec/plan predicted.
2. If this phase revealed a spec ambiguity or gap, run `/seed:spec-sync` before
   continuing (see `${CLAUDE_PLUGIN_ROOT}/context/spec-first.md`) rather than letting the corpus drift.
3. Write execution state: `Current Step: 6/6`.

Return: proceed to step 6.
