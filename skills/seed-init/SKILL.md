---
name: seed-init
description: Initialize a repository's SEED memory bank and deterministic project guard.
---

# SEED init

Inspect the repository and initialize `memory-bank/` using the current schema.
Record real Git facts and detected stack, install the project commit guard and
initialize the local cache. Preserve existing files, ask before overwriting
truth-bearing project configuration, and report every created file.
